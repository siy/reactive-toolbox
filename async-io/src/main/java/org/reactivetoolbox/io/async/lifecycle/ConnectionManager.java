package org.reactivetoolbox.io.async.lifecycle;

import org.reactivetoolbox.io.async.context.ConnectionContext;

import java.util.function.Consumer;

public class ConnectionManager implements Consumer<ConnectionContext> {
    //TODO: finish it (start reader lifecycle, register connection, etc);

    @Override
    public void accept(final ConnectionContext connectionContext) {

    }
}
